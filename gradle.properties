# This project targets JDK 25 (see build.gradle.kts java.toolchain).
# Requires Lombok 1.18.46+ (pinned in build.gradle.kts) and Gradle 9+ for the
# wrapper/build tooling itself to read JDK 25 class files.
#
# If Gradle can't auto-detect a JDK 25 install on your machine, point it at
# one via ~/.gradle/gradle.properties (user-level, not this file) with:
#   org.gradle.java.installations.paths=/path/to/your/jdk25
